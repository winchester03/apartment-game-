import {createRoom} from "./room.js";import {createPlayerController} from "./player.js";const canvas=document.getElementById("gameCanvas"),message=document.getElementById("message"),tapMarker=document.getElementById("tapMarker"),standButton=document.getElementById("standButton");const engine=new BABYLON.Engine(canvas,true,{preserveDrawingBuffer:true,stencil:true});const scene=new BABYLON.Scene(engine);scene.clearColor=new BABYLON.Color4(.035,.04,.05,1);const world=createRoom(scene);const player=createPlayerController({scene,canvas,message,tapMarker,standButton,world});engine.runRenderLoop(()=>{player.update();scene.render()});addEventListener("resize",()=>engine.resize());document.addEventListener("gesturestart",e=>e.preventDefault(),{passive:false});